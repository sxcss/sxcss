{
  "name": "test-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn install && nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint:style": "stylelint **/*.{vue,css} --ignore-path .gitignore",
    "lint": "yarn lint:style"
  },
  "dependencies": {
    "@naked-ui/vue": "^0.0.16",
    "core-js": "^3.6.5",
    "nuxt": "^2.14.6"
  },
  "devDependencies": {
    "@nuxtjs/stylelint-module": "^4.0.0",
    "fibers": "^5.0.0",
    "sass": "^1.30.0",
    "sass-loader": "^10.1.0",
    "stylelint": "^13.7.2",
    "stylelint-config-standard": "^20.0.0"
  }
}
